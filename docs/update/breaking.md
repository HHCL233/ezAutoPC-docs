# 不兼容提醒

在 ezAutoPC v4(Next) 中，以下内容存在较大改动：

::: details 工具调用格式
提示词调用格式被移除。
:::

::: details 配置存放位置
配置存放位置从项目根目录改为`~/.ezautopc/config.json`。
:::

::: details MCP配置
MCP配置由`mcp_servers`更名为`_mcp_servers`，更新后需自行迁移。
:::