# WebUI

WebUI 具有对话、配置等功能。

## 访问

当启动 ezAutoPC 之后，你可以通过在浏览器中访问 `http://localhost:5000/` 来访问 WebUI。

> [!TIP]
> 如果你在使用 `npm run dev` 启动 WebUI，则应当访问 `http://localhost:8080/`。

> [!TIP]
> 如果你正在云服务器上部署 ezAutoPC，需要将 localhost 自行替换为服务器 IP 地址。

## 登录

首次登录时输入的密码将自动作为账号 `admin` 的密码。